{
  "name": "tweet-code",
  "version": "1.0.0",
  "description": "",
  "main": "server/server.js",
  "scripts": {
    "install": "npm --prefix client install && npm --prefix server install",
    "build": "npm --prefix client run build",
    "seed": "node server/seeds/seed.js",
    "deploy": "npm run install && npm run build && npm run seed"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^5.1.0",
    "dotenv": "^16.4.5"
  },
  "dependencies": {
    "apollo-upload-client": "^18.0.1",
    "prop-types": "^15.8.1"
  }
}
